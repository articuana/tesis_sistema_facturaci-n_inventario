const userModel = {
  table: 'users',
  fields: ['id', 'username', 'first_name', 'last_name', 'name', 'email', 'identification', 'password_hash', 'role', 'is_active', 'joined_at', 'phone', 'location']
};

export default userModel;
